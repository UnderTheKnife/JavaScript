function task_1(a) {
    (a > 0 && a < 5) ? alert('Верно') : alert('Неверно');
}

function task_2(a) {
    (a === 0 || a === 2) ? a += 7 : a /= 10;
    alert(a);
}
function task_3(a, b) {
    (a <= 0 && b >= 3) ? alert(a + b) : alert(a - b);
}